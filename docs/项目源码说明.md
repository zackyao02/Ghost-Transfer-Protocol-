# 项目源码说明

当前 GitHub 仓库是团队唯一协作入口，承载项目规划、技术协议、协作文档和后续 Unity 源码。

团队主仓库：

```text
https://github.com/zackyao02/Ghost-Transfer-Protocol-.git
```

团队分支和目标项目路径：

```text
master
unity/ai-project/YaowuDemo
```

Unity 源码迁移来源为：

```text
https://gitee.com/Zack02/please-god.git
```

该 Gitee 仓库只允许读取和迁移，不再作为团队开发入口。

## 迁移状态

- 迁移基线：Gitee `master`，提交 `818044ba8f2be7754dda27ae9fad1fba0783d871`。
- 已迁入：`Assets/`、`Packages/`、`ProjectSettings/`。
- 已排除：旧仓库中的 Unity Hub、Editor/运行时文件、个人 `.vscode/` 与 `UserSettings/`。
- 已将 `Assets/Scenes/SampleScene.unity` 加入构建场景列表。
- 工程要求 Unity `2022.3.17f1c1`；当前机器未安装 Unity Editor，因此打开、编译和 Windows 构建验证仍待完成。
